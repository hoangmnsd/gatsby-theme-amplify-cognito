module.exports = ({
  region = '',
  userPoolId = '',
  userPoolWebClientId = '',
  doNotAuthenticate = []
}) => ({
  siteMetadata: {
    title: "Gatsby Theme Amplify with Cognito",
  },
  plugins: [

  ],
})
